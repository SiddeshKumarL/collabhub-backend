# Database Configuration
spring.datasource.url=jdbc:postgresql://localhost:5432/collabhub_db
spring.datasource.username=postgres
spring.datasource.password=Siddesh@31
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Server Configuration
server.port=8080

# JWT Configuration
jwt.secret=yourSecretKeyHere123456789012345678901234567890
jwt.expiration=86400000

# CORS Configuration
cors.allowed-origins=http://localhost:8081,http://localhost:8080
